/**
 * @file src/app/(auth)/layout.js
 * @description
 * Layout du groupe de routes d'authentification.
 */

/**
 * Layout appliqué aux pages d'authentification.
 * Rend directement le contenu enfant.
 *
 * @param {Object} props Propriétés du composant
 * @param {React.ReactNode} props.children Contenu de la route
 * @returns {React.ReactNode} Contenu enfant rendu tel quel
 */

export default function AuthLayout({ children }) {
	return children;
}
